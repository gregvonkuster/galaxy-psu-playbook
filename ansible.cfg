[defaults]
interpreter_python = /usr/bin/python3
#inventory = hosts
retry_files_enabled = false
vault_password_file = .vault-password.txt
roles_path = roles
transport = ssh

[ssh_connection]
pipelining = true
